# 手動検証チェックリスト — 2026-08-06

対象: このセッションで入れた変更のうち **機械で検証できないもの**だけを集約する。
機械で固定できた分は unit 284 / e2e 15 に入っているので、ここには**書かない**
(重複させると「どこまで自動化されているか」が読めなくなる)。

各項目は実施後 `- [ ]` を `- [x]` にし、FAIL なら理由を追記する。**勝手に PASS にしない。**

## 前提: `pnpm build` → ⟳ → 版数確認

1. **`pnpm build`** を実行する (同期フォルダへの展開まで自動で走る)。
2. `chrome://extensions` で拡張の **⟳ (更新)** を押す。`contextMenus` 権限を追加したので、
   これをしないと右クリックメニューは出ない。
3. **拡張カードの版数が上がっているか**を見る。上がっていなければ ⟳ が効いていない。

> 実績: 以前は build と同期が別コマンドで、3 日前のビルドを見せて「右クリックが出ない」と
> 報告された。**権限を追加した回は同期していないと ⟳ だけでは絶対に動かない** (manifest が古いまま)。

---

## A. 右クリックメニュー

- [ ] **A1. メニューが出る**: localhost または許可済みサイトで要素を右クリック。
  期待: 「この要素を検査」「この要素のソースをエディタで開く」の 2 項目が出る。
- [ ] **A2. 動作範囲外では出ない**: **未許可**の https サイトで右クリック。
  期待: 2 項目が**出ない**(出て押せると無反応になるため、出ないことが正しい)。
- [ ] **A3. 許可した瞬間に出る**: A2 のサイトで popup から「現在のサイトで有効化」→ もう一度右クリック。
  期待: 項目が出る(`permissions.onAdded` でメニューを作り直しているため、拡張の再読込は不要)。
- [ ] **A4. モード OFF から 1 アクション**: インスペクト OFF の状態で「この要素を検査」を押す。
  期待: モードが ON になり、**右クリックした要素**のバッジが出る(別の要素ではない)。
- [ ] **A5. 許可前から開いていたタブ**: 全サイト許可を付ける **前**から開いていたタブで右クリック → 「この要素を検査」。
  期待: 動く(背後で注入して再送している)。FAIL なら無反応になるので、そのタブをリロードして再確認し記録する。

## B. エディタ起動 (scheme 起動は自動検証不可)

- [ ] **B1. dev で実際に開く**: localhost の React **開発**ビルドで要素にホバー → ⌘/Ctrl+Click。
  期待: Cursor が**実際に開き**、該当ファイルの該当行にカーソルが立つ。トーストに「エディタでソースを開いています…」。
- [ ] **B2. 右クリックからも開く**: 同じ要素を右クリック → 「この要素のソースをエディタで開く」。
  期待: B1 と同じ結果(モード ON は不要)。
- [ ] **B3. production は理由が出る**: デプロイ済みサイトで ⌘/Ctrl+Click。
  期待: 何も起きないのではなく「本番サイト(公開ビルド)のため、コードへのジャンプは使えません」が出る。
- [ ] **B4. 素の DOM は理由が出る**: 非 React サイトで同じ操作。
  期待: 「この要素は React コンポーネントではないため、開くソースファイルがありません」。
- [ ] **B5. パスマッピング**: popup の「開発者向け」→ パスマッピングに `from=to` を 1 行入れ、
  ブラウザが報告するパスと手元のパスが違う環境 (Docker / リモート) で B1 を試す。
  期待: 置換後の正しいファイルが開く。
- [ ] **B7. エディタが開かないときのフォールバック**: 存在しないスキームを指すよう
  「カスタム URL…」に `notarealeditor://file{file}:{line}` を入れて B1 を試す。
  期待: 1.2 秒後に「何も開きませんでした…」+ **[パスをコピー]** ボタンが出る。押すと
  `/src/App.tsx:42` 形式がクリップボードに入る。**無反応で終わらないこと。**
- [ ] **B6. カスタム URL**: エディタ選択を「カスタム URL…」にし、`zed://file{file}:{line}:{column}` 等を入れる。
  期待: 入力欄が現れ、保存され、そのエディタが開く。

## C. Alt+Click の描画元リスト

- [ ] **C1. パネルが出る**: dev の React ページでホバー → Alt+Click。
  期待: 「描画元 (クリックでエディタへ)」パネルが出て、コンポーネント名と file:line が並ぶ。
- [ ] **C2. 行クリックでエディタ**: パネルの行をクリック。期待: その**ファイル**が開き、パネルが閉じる。
- [ ] **C3. 空のときに理由が出る**: 素の DOM 要素 / production ページで Alt+Click。
  期待: タイトルだけの空の箱にならず、「表示できる描画元がありません…」が出る。
- [ ] **C4. Esc で閉じる**: パネルが開いている状態で Esc。期待: パネルだけ閉じ、モードは維持される。

## D. Web Components (closed shadow root)

e2e は `attachShadow` を open 強制しているため、**closed の実挙動は自動検証できない**。

- [ ] **D1. open な shadow root**: 内部要素にホバー。期待: 内部要素の値が出る(ホストの値ではない)。
- [ ] **D2. closed な shadow root**: 内部要素にホバー。期待: ホスト要素の値が出て、**ホストとして**表示される
  (内部の値をホストの値として出していない = 誤答していない)。

## E. blob: トップレベルタブ (自動検証不可)

popup を通常タブで開く e2e 方式では `tabs.query({active:true})` が popup 自身を返すため、
blob タブを対象にできない。

- [ ] **E1. URL 不明タブの導線**: 全サイト許可を付けた状態で、`URL.createObjectURL` で作った
  HTML を新規タブで開き popup を開く。
  期待: 「このタブは URL を返しません…」と出て、有効化ボタンが**押せる**。押すと検査が始まる。
  FAIL 条件: 「このページでは有効化できません」と出て諦めさせる / 押しても無反応。

## E-2. v1 の配線から外した UI が出ていないこと

掲載文・データ申告を狭めた前提が崩れていないかの目視確認 (e2e でも count 0 を固定済み)。

- [ ] **E2. popup に無いこと**: トークンカバレッジ (「この画面を測定」) / AI デザイン監査 /
  表示設定 (CSS 変数名優先・MUI テーマ自動検出・バッジの情報量) / トークン JSON 貼り付け。
  期待: **どれも表示されない**。popup は「このサイトで使う / モード / 開発者向け / 使い方」の 4 群のみ
- [ ] **E3. MUI サイトで照合が動く**: MUI を使った実サイト (localhost でも本番でも) で
  ホバー。期待: バッジのチップに**テーマ由来のトークン名**が緑で出る (貼り付けは不要)。
  検出時にトースト「テーマから N 色 / M サイズを読み込みました」

## F. 有効化直後の i18n (決定論バグの実機確認)

- [ ] **F1. 日本語 UI で日本語のまま**: Chrome の表示言語を日本語にし、**未許可**の production サイトで
  「現在のサイトで有効化」→ リロードせずにホバー。
  期待: ページ内のトースト・バッジのラベルが**日本語**で出る(以前は英語で固定されていた)。

## F-2. iframe を含むページ (実キーの Esc は機械検証していない)

e2e は「どのフレームが Esc を受けたか」を曖昧にしないため合成イベントで検証している。
**実キーの経路とフォーカスの絡みは人間しか確認できない。**
確認用ページ: iframe を持つ実サイト (埋め込み動画・広告・プレビューのあるページで可)。

- [ ] **F2. Esc 1 回で iframe 内のクリックが復活する**: モードを ON → iframe の中を
  ホバーして計測値が出ることを確認 → **親のページをクリックせずに** Esc を 1 回押す。
  期待: モードピルが消え、**iframe 内のリンク / ボタンが普通に押せる**
  (以前は iframe だけ ON のまま残り、リロードするまで押せなかった)。
- [ ] **F3. iframe 内で Esc を押しても全部切れる**: 再度 ON → iframe の中をクリック
  (握りつぶされる) → iframe にフォーカスがある状態で Esc。
  期待: 親のモードピルも消え、親のページのクリックも復活する。
- [ ] **F4. ピルとトーストが 1 つだけ**: iframe が複数あるページで ON。
  期待: モードピル (「Inspecting — Esc to exit」) と ON のトーストが**画面に 1 つだけ**。
- [ ] **F5. 連打で位相がずれない**: ON → OFF → ON → OFF をショートカットで 4 回。
  期待: 毎回すべてのフレームで揃う (どこかのフレームだけ ON のまま残らない)。

## G. セキュリティ (偽装への防御)

- [ ] **G0-2. ページが注入した辞書で「一致」が出ない**: 任意のページで DevTools のコンソールから
  `window.postMessage({source:'domdom-inspector-bridge',type:'tokens',payload:{colors:[{name:'FORGED',r:255,g:0,b:0,a:1}],sizes:[]}},'*')`
  を実行し、赤い要素 (`#ff0000`) をホバーする。
  期待: バッジに **`FORGED` が出ない** (辞書の供給元は MUI テーマの自動検出だけ)。

- [ ] **G0. 合成イベントでは動かない**: 任意のページで DevTools のコンソールから
  `document.body.dispatchEvent(new MouseEvent('contextmenu',{bubbles:true}))` を実行し、続けて
  `window.postMessage({source:'domdom-inspector-bridge',type:'open-editor-at-context'},'*')` を送る。
  期待: **何も起きない** (エディタが開かない / トーストも出ない)。その後、実際に右クリック →
  メニューから「ソースをエディタで開く」を選ぶと動く。

## G-2. 表示崩れ (英日 × 幅)

- [ ] **G1. popup 320px**: 「開発者向け」を開き、パスマッピング textarea とカスタム URL 入力が
  はみ出さず、ヒント文が読める。en / ja 両方。
- [ ] **G2. 描画元パネル**: 長いコンポーネント名 + 長いファイル名で、パネルが画面外へ出ない。

---

## 参照

- 前回のチェックリスト: `docs/manual-verification-20260707.md`
  (⚠️ ツリー / レンダー計測の項目を含むが、それらは v1 の配線から外してあるため実施不能)
- 設計の背景: `docs/design-coverage-screen.md` / `docs/ROADMAP.md`
